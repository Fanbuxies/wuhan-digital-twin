<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuhan.twin.alarm.mapper.AlarmMapper">

    <insert id="batchInsert">
        INSERT INTO t_alarm (device_id, object_type, alarm_type, alarm_level,
                             alarm_value, status, occur_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.deviceId}, #{item.objectType}, #{item.alarmType}, #{item.alarmLevel},
             #{item.alarmValueJson}::jsonb, 'PENDING', #{item.occurTime})
        </foreach>
    </insert>

    <select id="selectPendingDeviceIds" resultType="java.lang.Long">
        SELECT DISTINCT a.device_id
        FROM t_alarm AS a
        WHERE a.status = 'PENDING'
          AND a.object_type = #{objectType}
    </select>

    <!-- 告警表同样以 object_type 区分监测对象类型，删除必须带类型 -->
    <delete id="deleteByDeviceId">
        DELETE FROM t_alarm
        WHERE object_type = #{objectType}
          AND device_id = #{deviceId}
    </delete>

</mapper>
